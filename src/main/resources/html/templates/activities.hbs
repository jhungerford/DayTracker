{{partial "activityInput"}}

{{#each groupByDay}}
	<div class="row day">
		<div class="small-2 columns">
			<h4>{{logDate day}}</h4>
		</div>
		<div class="small-10 columns">
			<ul class="no-bullet">
				{{#each activities itemController="activity"}}
					{{#if isEditing}}
						<li>{{partial "activityEdit"}}</li>
					{{else}}
						<li class="activity" {{action 'toEdit' id}}>{{text}}</li>
					{{/if}}
				{{/each}}
			</ul>
		</div>
	</div>
{{/each}}
